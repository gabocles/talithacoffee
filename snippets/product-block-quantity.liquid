{%- liquid
  assign label_class = 'sr-only'
  if block.settings.show_label
  assign label_class = 'block font-secondary text-scheme-text text-sm'
  endif
-%}
<div
 
  class="{{ block_base_classes }}"
  x-data="ThemeComponent_Quantity()"
  {{ block.shopify_attributes }}
>
  <div class="lg:flex lg:items-center lg:justify-start">
    <label class="{{ label_class }}  lg:w-1/4 xl:w-1/5 lg:shrink-0 pr-2.5" for="Quantity-{{ section.id }}">{{ 'products.product.quantity' | t }}</label>
    <div class="lg:mt-0 lg:flex-1">
      <div class="flex flex-nowrap justify-start items-center">
        <button
          class="quantity"
          aria-label="&minus;"
          @click.prevent.stop="decreaseQuantity"
        >
          {% render 'icon-subtract' %}
        </button>
        <input
          type="number"
          class="quantity input--no-border block appearance-none text-center border-0 bg-transparent text-base max-w-[3rem] p-2 text-scheme-text"
          form="{{ product_form_id }}"
          id="Quantity-{{ section.id }}"
          name="quantity"
          x-model="quantity"
          x-ref="quantityInput"
        >
        <button
          class="quantity"
          aria-label="&plus;"
          @click.prevent.stop="increaseQuantity"
        >
          {% render 'icon-add' %}
        </button>
      </div>
    </div>
  </div>
</div>

{% unless request.design_mode %}
  <script src="{{ 'modules-quantity.bundle.min.js' | asset_url }}" type="module"></script>
{% endunless %}
